# Shared environment variables used across all workflows
# Copy this to .env and fill in your values
#
# This is the PRIMARY source of env vars. All workflows load this file first.
# Individual workflows can override these values in their own .env file.

# LLM Providers
OPENAI_API_KEY=sk-your-openai-key
OPENROUTER_API_KEY=sk-or-your-openrouter-key

# Modal (for deploying workflows)
# Run `modal setup` to configure automatically, or set manually:
# MODAL_TOKEN_ID=your-token-id
# MODAL_TOKEN_SECRET=your-token-secret

# Shared bearer token for securing Modal webhooks
MODAL_BEARER_TOKEN=your-bearer-token